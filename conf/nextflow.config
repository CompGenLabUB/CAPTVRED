
params{
    // Mandatory CLI options
    set_seqs  =  null  // Fasta file for set species is stores (full path)
    setname   =  "targetset"
    refseqs   =  null  // refseqs directory

      // Filtering Step
        filtDB  =  "nr_euk"  // database for filtering
        
      // Mapping Step

        // Download RVDB

         rvdb_dir      =  "db/rvdb_nt"   //blast_refseqs_dir
         rvdb_link     =  "https://rvdb.dbi.udel.edu/download/"
         db_name       =  "C-RVDBvCurrent.fasta.gz"
         rvdb_update   =  false
         merge_update  =  false

        // RVDB for kaiju taxonomy

         blast_kaiju_db     =  "rvdb"

        // Prepare RVDB for bowtie 

        // Prepare RVDB for Blast

}